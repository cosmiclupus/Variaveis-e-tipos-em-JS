let nome =  "João";

let sobrenome = "Pedro";

typeof nome

nome.concat(sobrenome)

let concatenado = nome.concat(sobrenome);

typeof concatenado

concatenado.length

let exemplo = new String("blablabla");

typeof exemplo

concatenado = nome + " " + sobrenome;

concatenado = nome + "\n" + sobrenome;

concatenado = nome + "\n\n" + sobrenome;

concatenado = `${nome} ${sobrenome}`;

let frase = "Bom dia senhor!";
frase.split("");
frase.startsWith("O");
frase.includes("tudo");
frase.startsWith("R");
frase.endsWith("?");

frase.replace(",", "!");

let stringModificada = frase.replace(",","!")

